pip install numpy
# must have open-mpi installed ( on macOS, use  `brew install open-mpi` )
pip install stable-baselines==2.7.0
pip install keras==2.4.3
pip install tensorflow==1.14.0
pip install tqdm
pip install colorama